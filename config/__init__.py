"""Config package: constants and mapping tables that must not be hardcoded.

Per brief Section 19: model names, provider order, prompt templates, and
platform field mappings live here, never inline in logic modules.
"""
